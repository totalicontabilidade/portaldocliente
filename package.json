{
  "name": "totali-portal-cliente",
  "private": true,
  "description": "Portal do Cliente da Totali: testes das regras de segurança no emulador do Firebase",
  "scripts": {
    "test:regras": "firebase emulators:exec --only firestore,storage \"node --test tests/\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase-tools": "^14.0.0"
  }
}
